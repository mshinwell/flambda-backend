(include_subdirs unqualified)

(library
 (name arm64_binary_emitter)
 (wrapped true)
 (ocamlopt_flags
  (:standard
   (:include %{project_root}/ocamlopt_flags.sexp)))
 (libraries ocamlcommon asm_targets))
